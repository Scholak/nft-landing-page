import styled from 'styled-components'
import { SectionWrapper } from '../shared/Section.styles'

export const CreatorsSectionWrapper = styled(SectionWrapper)`
	img {
		display: block;
		max-width: 100%;
	}
`
